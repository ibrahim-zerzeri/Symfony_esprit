<!DOCTYPE html>
<html>
<head>
    <title>Update Author</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Update Author</h1>
        
        <form method="post">
            <div class="mb-3">
                <label for="username" class="form-label">Username</label>
                <input type="text" class="form-control" id="username" name="username" value="{{ author.username }}" required>
            </div>
            
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email" value="{{ author.email }}" required>
            </div>
            
            <div class="mb-3">
                <label for="nb_books" class="form-label">Number of Books</label>
                <input type="number" class="form-control" id="nb_books" name="nb_books" value="{{ author.nbBooks }}" min="0" required>
            </div>
            
            <button type="submit" class="btn btn-primary">Update Author</button>
            <a href="{{ path('showAllAuthors') }}" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</body>
</html>