<!DOCTYPE html>
<html>
<head>
    <title>Update Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Update Book: {{ book.title }}</h1>
        
        <div class="card">
            <div class="card-body">
                <form method="post">
                    <div class="mb-3">
                        <label for="title" class="form-label">Title:</label>
                        <input type="text" id="title" name="title" value="{{ book.title }}" class="form-control" required>
                    </div>
                    
                    <div class="mb-3">
                        <label for="publication_date" class="form-label">Publication Date:</label>
                        <input type="date" id="publication_date" name="publication_date" 
                               value="{{ book.publicationDate|date('Y-m-d') }}" class="form-control" required>
                    </div>
                    
                    <div class="mb-3">
                        <label for="category" class="form-label">Category:</label>
                        <select id="category" name="category" class="form-control" required>
                            <option value="Fiction" {{ book.category == 'Fiction' ? 'selected' : '' }}>Fiction</option>
                            <option value="Non-Fiction" {{ book.category == 'Non-Fiction' ? 'selected' : '' }}>Non-Fiction</option>
                            <option value="Science" {{ book.category == 'Science' ? 'selected' : '' }}>Science</option>
                            <option value="Technology" {{ book.category == 'Technology' ? 'selected' : '' }}>Technology</option>
                            <option value="History" {{ book.category == 'History' ? 'selected' : '' }}>History</option>
                            <option value="Biography" {{ book.category == 'Biography' ? 'selected' : '' }}>Biography</option>
                            <option value="Fantasy" {{ book.category == 'Fantasy' ? 'selected' : '' }}>Fantasy</option>
                            <option value="Mystery" {{ book.category == 'Mystery' ? 'selected' : '' }}>Mystery</option>
                            <option value="Romance" {{ book.category == 'Romance' ? 'selected' : '' }}>Romance</option>
                            <option value="Children" {{ book.category == 'Children' ? 'selected' : '' }}>Children</option>
                            <option value="Other" {{ book.category == 'Other' ? 'selected' : '' }}>Other</option>
                        </select>
                    </div>
                    
                    <div class="mb-3">
                        <label for="enabled" class="form-label">Enabled:</label>
                        <select id="enabled" name="enabled" class="form-control">
                            <option value="1" {{ book.enabled ? 'selected' : '' }}>Yes</option>
                            <option value="0" {{ not book.enabled ? 'selected' : '' }}>No</option>
                        </select>
                    </div>
                    
                    <button type="submit" class="btn btn-primary">Update Book</button>
                    <a href="{{ path('showAllBooks') }}" class="btn btn-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</body>
</html>