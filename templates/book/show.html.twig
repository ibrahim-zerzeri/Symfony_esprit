<!DOCTYPE html>
<html>
<head>
    <title>Show Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Book Title: {{ title }}</h1>
        
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Book Information</h5>
                <p class="card-text">You are viewing the book: <strong>{{ title }}</strong></p>
            </div>
        </div>
        
        <a href="{{ path('app_book') }}" class="btn btn-primary mt-3">Back to Book Home</a>
    </div>
</body>
</html>