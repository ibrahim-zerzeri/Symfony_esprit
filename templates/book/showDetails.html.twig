<!DOCTYPE html>
<html>
<head>
    <title>Book Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Book Details</h1>
        
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ book.title }}</h5>
                <p class="card-text">
                    <strong>ID:</strong> {{ book.id }}<br>
                    <strong>Publication Date:</strong> {{ book.publicationDate|date('Y-m-d') }}<br>
                    <strong>Category:</strong> <span class="badge bg-info text-dark">{{ book.category }}</span><br>
                    <strong>Enabled:</strong> 
                    <span class="badge {{ book.enabled ? 'bg-success' : 'bg-danger' }}">
                        {{ book.enabled ? 'Yes' : 'No' }}
                    </span><br>
                    <strong>Author:</strong> {{ book.author ? book.author.username : 'No Author' }}
                </p>
            </div>
        </div>
        
        <a href="{{ path('showAllBooks') }}" class="btn btn-primary mt-3">Back to Books List</a>
    </div>
</body>
</html>