<!DOCTYPE html>
<html>
<head>
    <title>Book Statistics</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Book Statistics</h1>
        
        <div class="row">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Romance Books Count</h5>
                        <p class="card-text">
                            Number of books in "Romance" category: 
                            <span class="badge bg-primary fs-6">{{ romanceBookCount }}</span>
                        </p><!--
                        {# Fix: only show button if route exists #}
                        {% if 'search_romance_books' in app.request.attributes.get('_route') %}
                            <a href="{{ path('search_romance_books') }}" class="btn btn-info">View Romance Books</a>
                        {% else %}
                            <button class="btn btn-secondary" disabled>View Romance Books (coming soon)</button>
                        {% endif %}-->
                    </div>
                </div>
            </div>
        </div>

        <div class="mt-4">
            <h3>Books Published Between 2014-01-01 and 2018-12-31</h3>
            
            {% if booksBetweenDates is not empty %}
                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <thead class="table-dark">
                            <tr>
                                <th>ID</th>
                                <th>Title</th>
                                <th>Publication Date</th>
                                <th>Category</th>
                                <th>Author</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for book in booksBetweenDates %}
                            <tr>
                                <td>{{ book.id }}</td>
                                <td>{{ book.title }}</td>
                                <td>{{ book.publicationDate|date('Y-m-d') }}</td>
                                <td><span class="badge bg-info text-dark">{{ book.category }}</span></td>
                                <td>{{ book.author ? book.author.username : 'No Author' }}</td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <div class="alert alert-warning" role="alert">
                    No books found in this date range.
                </div>
            {% endif %}
        </div>
        
        <a href="{{ path('app_book') }}" class="btn btn-primary mt-3">Back to Book Home</a>
          <a href="{{ path('showAllBooks') }}" class="btn btn-secondary mt-3">Back to Book table</a>
    </div>
</body>
</html>
