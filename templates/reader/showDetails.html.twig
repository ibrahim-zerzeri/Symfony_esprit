<!DOCTYPE html>
<html>
<head>
    <title>Reader Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Reader Details</h1>
        
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Reader #{{ reader.id }}</h5>
                <p class="card-text"><strong>Username:</strong> {{ reader.username }}</p>
                <p class="card-text">
                    <strong>Books Read:</strong> 
                    <span class="badge bg-primary">{{ reader.books|length }}</span>
                </p>
                
                {% if reader.books|length > 0 %}
                <h6>Books:</h6>
                <ul class="list-group">
                    {% for book in reader.books %}
                    <li class="list-group-item">{{ book.title }} ({{ book.category }})</li>
                    {% endfor %}
                </ul>
                {% endif %}
            </div>
        </div>
        
        <a href="{{ path('showAllReaders') }}" class="btn btn-primary mt-3">Back to Readers List</a>
    </div>
</body>
</html>